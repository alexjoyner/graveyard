import {expect} from 'chai';
import {init} from './index';


describe('server config', () => {
	describe('init index',() => {
		it('should be truthy', () => {
			expect(2+2).to.equal(4);
		})
	});
});

