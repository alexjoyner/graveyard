apps:
  - script   : ./server._startup
    name     : 'your-project-name'
    exec_mode:
      NODE_ENV: 'production'
    env:
      NODE_ENV: 'production'
    env_production:
      NODE_ENV: 'production'
