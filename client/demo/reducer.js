import { POST_PREORDER, POST_COMMENT } from './actions';

const INITIAL_STATE = {};

const demoReducer = (state = INITIAL_STATE, action) => {
	switch(action.type) {
		default:
			return state;
	}
};

export {demoReducer};
