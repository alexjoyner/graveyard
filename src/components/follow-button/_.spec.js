import {renderComponent, expect} from '../../js/test_helper';
import followButton from './';

// Used describe to group together similar tests
describe('RENAME_THIS', () => {
	let component;

	beforeEach(() => {
		component = renderComponent(followButton);
	});
});
