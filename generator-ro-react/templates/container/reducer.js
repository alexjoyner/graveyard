import {} from './actions';

const INITIAL_STATE = {};

const <%= containerName %>Reducer = (state = INITIAL_STATE, action) => {
	switch(action.type) {
		default:
			return state;
	}
};

export {<%= containerName %>Reducer};
