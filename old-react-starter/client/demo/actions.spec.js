import {expect} from 'ro-dev-utils';
import {CONTAINER_NAME} from './_config';
import {} from './actions';

// Used describe to group together similar tests
describe(CONTAINER_NAME + ' actions', () => {});
