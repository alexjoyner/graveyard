apps:
  - script   : ./server/server.js
    name     : <% appName %>
    exec_mode:
      NODE_ENV: 'production'
    env:
      NODE_ENV: 'production'
    env_production:
      NODE_ENV: 'production'
