import React, {Component} from 'react';
import './Home.css';
import {Layout} from "../../features/layout";

class Home extends Component {
    render() {
        return (
            <Layout>
            </Layout>
        );
    }
}

export default Home;
